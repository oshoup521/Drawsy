# Backend Environment Configuration for NGROK testing

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=osho
DB_DATABASE=scribbl_ai

# Application Configuration
PORT=3000
NODE_ENV=development

# LLM Service Configuration (can use localhost or ngrok for LLM service)
# For ngrok testing, either use localhost:8000 (if LLM runs locally) or create ngrok tunnel for LLM service
# Replace with your actual LLM service ngrok URL if using ngrok for LLM service
LLM_SERVICE_URL=http://localhost:8000

# CORS Configuration - Allow both local and ngrok origins
CORS_ORIGINS=http://localhost:3001,http://localhost:3000,https://crack-leading-feline.ngrok-free.app,https://intent-knowing-ape.ngrok-free.app

# Environment Type
ENVIRONMENT=ngrok
